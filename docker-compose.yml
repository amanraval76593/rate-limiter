version: "3.9"

services:
  redis:
    image: redis:7
    container_name: rate_limiter_redis
    ports:
      - "6382:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes


volumes:
  redis-data:
